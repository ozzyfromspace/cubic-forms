# ---------------------------------------------
# 1) Node-Related Files
# ---------------------------------------------
node_modules/
*.log
pnpm-debug.log*
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# ---------------------------------------------
# 2) Source & Build Files
# ---------------------------------------------
# Typically you want to exclude raw sources if you only distribute compiled output
src/
scripts/
build/
# Keep /dist if that’s where your compiled output resides, e.g.
# !dist/
# global-nuxt.d.ts is not required in the consumer app, which knows about nuxt
dist/global-nuxt.d.ts

# ---------------------------------------------
# 3) Tools / Config
# ---------------------------------------------
.vscode/
.idea/
*.swp
vite.config.*
tsconfig.json
nuxt.config.*
.github/

# Optional, if you don’t want to publish these lock files
*.lock

# ---------------------------------------------
# 4) Testing
# ---------------------------------------------
test/
tests/
coverage/
.nyc_output/
.husky/

# ---------------------------------------------
# 5) OS / Editor Junk
# ---------------------------------------------
.DS_Store
Thumbs.db
*.seed
*.pid
*.pid.lock

# ---------------------------------------------
# 6) Local Environment
# ---------------------------------------------
.env
.env.*.local
*.local

# ---------------------------------------------
# 7) Build, Cache, and Other Output
# ---------------------------------------------
.cache/
.vite/
.nuxt/
tmp/
*.tmp
*.temp

# ---------------------------------------------
# 8) Additional Exclusions
# ---------------------------------------------
# If we ever make @cubic/forms monorepo-based or implement subfolders that don't want published,
# exclude them here:
# packages/*/dist/
# or any custom directories not needed at runtime.